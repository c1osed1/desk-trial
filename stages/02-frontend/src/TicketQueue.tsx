import type { ReactElement } from 'react';
import type { TicketApi } from './types.ts';

/** TODO */
export function TicketQueue(_props: { api: TicketApi }): ReactElement {
  throw new Error('TODO: implement TicketQueue');
}
