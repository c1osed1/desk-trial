# Этап 2 — фронт

Очередь заявок. Файлы с TODO.

Подписи:

- pc_request → ПК, sms_code → SMS-код, balance → Баланс
- приоритет 1 / 2 / 3 → срочно / обычно / низкий

Загрузка, пустой список и ошибка — разные состояния, не мешай их.

Фильтр по статусу на клиенте, без второго запроса.

«Взять» только у открытой. Пока берём — кнопка не жмётся.

Тесты ходят по ролям и подписям, не по классам.
