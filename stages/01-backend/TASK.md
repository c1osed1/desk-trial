# Этап 1 — бэк

Репа и Fastify уже есть. Пишешь `service.ts`, `handlers.ts`, `mix.ts`.

Очередь заявок клуба: ПК / SMS / баланс. Приоритет 1–3, если не передали — 2.

create, assign, complete, cancel, list.

В работе у человека не больше одной заявки. Чужую закрыть нельзя. list — сначала горячие, внутри одной горячести кто раньше пришёл.

HTTP через AppError. В 500 внутренности не светить.

`mixSignals(A, B)` — обычное умножение матриц. На больших не должно отваливаться по времени.
