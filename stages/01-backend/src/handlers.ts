import type { FastifyInstance } from 'fastify';
import type { TicketService } from './service.ts';

/** TODO */
export async function registerTicketHandlers(_app: FastifyInstance, _service: TicketService): Promise<void> {
  throw new Error('TODO: implement registerTicketHandlers');
}
